import * as React from 'react';
import { hot } from 'react-hot-loader/root';

export default hot(
    (): JSX.Element => {
        return (
            <div>
                <h1>Your Electron App</h1>
            </div>
        );
    }
);
